name: Ardor RunPod Probe

on:
  workflow_dispatch:
  push:
    paths:
      - "runpod/inspect/**"

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      RUNPOD_API_KEY: ${{ secrets.RUNPOD_API_KEY }}
      RUNPOD_S3_ACCESS_KEY_ID: ${{ secrets.RUNPOD_S3_ACCESS_KEY_ID }}
      RUNPOD_S3_SECRET_ACCESS_KEY: ${{ secrets.RUNPOD_S3_SECRET_ACCESS_KEY }}
      RUNPOD_NETWORK_VOLUME_ID: ${{ vars.RUNPOD_NETWORK_VOLUME_ID }}
      RUNPOD_DATACENTER_ID: ${{ vars.RUNPOD_DATACENTER_ID }}
      RUNPOD_S3_ENDPOINT: ${{ vars.RUNPOD_S3_ENDPOINT }}
      RUNPOD_PROVISIONING_TIMEOUT_MINUTES: ${{ vars.RUNPOD_PROVISIONING_TIMEOUT_MINUTES }}

    steps:
      - name: Check out Ardor
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install boto3
        run: python -m pip install --disable-pip-version-check boto3

      - name: Probe detached RunPod provisioning
        run: python scripts/runpod_provisioning_watchdog.py
